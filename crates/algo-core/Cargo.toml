[package]
name = "algo-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { workspace = true }
orderbook = { path = "../orderbook" }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
