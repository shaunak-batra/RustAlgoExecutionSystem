[package]
name = "database"
version = "0.1.0"
edition = "2021"

[dependencies]
orderbook = { path = "../orderbook" }
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
thiserror = "1.0"
